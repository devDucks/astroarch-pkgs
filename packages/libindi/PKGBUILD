# Maintainer: Mattia Procopio (astro.matto)  <matto.astro at gmail dot com>
pkgname=libindi
pkgver=2.2.0
pkgrel=2
pkgdesc='Indi core library'
arch=(x86_64 aarch64)
url='https://github.com/indilib/indi'
license=(LGPLv2)
depends=(cfitsio
         curl
         fftw
         gpsd
         gsl
         libev
         libftdi
         libgphoto2
         libjpeg-turbo
         libnova
         libraw
         libtheora
         libtiff	 
         libusb
         libuv
         rtl-sdr
         zlib)
makedepends=(boost
             cmake)
source=(https://github.com/indilib/indi/archive/refs/tags/v$pkgver.tar.gz)
sha256sums=('6b5a59496457986c37868ffa1ad06f41079707566a8929015e15494b218ecc7f')

build() {
  cmake -B $srcdir/build -DCMAKE_INSTALL_PREFIX=/usr \
	-DUDEVRULES_INSTALL_DIR=/usr/lib/udev/rules.d \
    	-DCMAKE_C_FLAGS="$CFLAGS -ffat-lto-objects" \
	-DCMAKE_CXX_FLAGS="$CXX_FLAGS -ffat-lto-objects -Wp,-U_GLIBCXX_ASSERTIONS" \
    	indi-$pkgver
  cmake --build $srcdir/build
}

package() {
  DESTDIR="$pkgdir" cmake --install build
}
